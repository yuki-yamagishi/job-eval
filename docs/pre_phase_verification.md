# Pre-Phase Verification Report (事前検証レポート)

各フェーズ着手前に実施する 4 軸（技術環境、UX、データ永続性、テスト自律性）の事前検証ログです。

---

## Phase 17: 品質ゲート（docCheck.js）における ADR 一覧整合性の自動検証機能の追加 (Issue #9)

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | `scripts/docCheck.js` が Node.js の `fs` / `path` で Windows / macOS / Linux 環境を問わず安全に動作すること。 | `path.resolve` / `path.join` によるクロスプラットフォームパス解決と、正規表現によるファイル名・ADR番号（`ADR-XXXX`）突合ロジックを実装。 |
| **2. UX・エッジケース** | 開発者やAIエージェントが ADR の目次登録を忘れた場合、どのファイルが未登録かをわかりやすくコンソールに出力すること。 | 未登録の ADR 一覧を `console.error` で具体的に箇条書き出力し、対処手順（`docs/adr/README.md への追記`）を明示。 |
| **3. 永続性・互換性** | 既存のドキュメント検査（`pre_phase_verification.md` 等）やビルドプロセスを破壊しないこと。 | 既存の3ファイル検査フローをそのまま維持し、ADR ディレクトリ検査を第2ステップとして安全に追加。 |
| **4. テスト容易性 & 自律性** | `scripts/docCheck.js` 単体実行および `npm run check` 全体で確実に検証が自律実行されること。 | `node scripts/docCheck.js` を実行し、全7件の ADR が正常認識されることを確認。 |

---

## Phase 16: 求人・プロファイルデータのスマートマージとコンフリクト解消 (Issue #7, ADR-0007)

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | PC[A, C] と スマホ[A, B] で求人Aのステータスが「書類選考」と「一次面接」で異なる場合、どちらを正とするかの競合（コンフリクト）。 | Last-Write-Wins (LWW) 原則を採用し、`metadata.updatedAt` または `analysisDate` の新しい方を採用。差分履歴（`evaluationHistory`）も重複排除マージ。 |
| **2. UX・エッジケース** | ユーザーがPCとスマホのどちらから同期を開始しても、操作中の求人が消えたり巻き戻ったりせず、全求人（A, B, C）が自動統合されること。 | `mergeJobs` と `mergeProfile` による和集合自動マージをストレージ受信時に透過的に実行。ユーザーへの特別な確認ダイアログや警告なしに完全自動解決。 |
| **3. 永続性・互換性** | マージ処理後の求人データがローカルストレージおよび Markdown 構造と 100% 整合し、データ破損が起きないこと。 | `JobAnalysisResult` および `UserProfile` の型定義に完全準拠した純粋関数として実装し、各フィールドの欠落を防止。 |
| **4. テスト容易性 & 自律性** | エッジケース（空配列、完全重複、完全差分、タイムスタンプ逆転、プロファイルスキル合体）が Vitest で 100% 単体テスト可能か。 | `tests/core/smartMerge.test.ts` を作成し、純粋関数の全ケースを網羅的に自動テスト。 |

---

## Phase 15: クラウドデータベースによる複数デバイス間リアルタイムデータ同期の実装 (Issue #4, ADR-0006)

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | クラウドDB接続時のレイテンシ、オフライン時の切断エラー、および外部SDK依存によるバンドルサイズ増加と初期化失敗リスク。 | Firestore `onSnapshot` によるネイティブリアルタイム購読を導入。未接続時や通信エラー時は自動的にローカルキャッシュおよび `LocalStorageAdapter` へ透過的フォールバックする防御設計。 |
| **2. UX・エッジケース** | 複雑なパスワード入力や認証なしで、PCとスマホを素早くペアリングできること。同期状態（同期中/オフライン/未設定）がUI上で一目でわかること。 | 「同期ルームID / 6桁ペアリングコード」方式を採用し、ワンクリックでQRコード表示・スマホ側で読み取り可能にする。ヘッダーに「🟢 クラウド同期中」「☁️ ローカル動作」のインジケーターを常設。 |
| **3. 永続性・互換性** | 既存の `JobAnalysisResult` および `UserProfile` データ構造を100%維持し、ローカルMarkdownエクスポートや既存データとの完全互換性を確保。 | `StorageAdapter` インターフェースに `subscribeJobs` / `subscribeProfile` / `setSyncRoom` などの購読メソッドを安全に拡張し、純粋なコアビジネスロジック（`src/core/`）には一切手を加えない。 |
| **4. テスト容易性 & 自律性** | 外部クラウドAPI（Firebase）に依存するテストがCIやローカル環境で失敗・ブロックされないこと。 | `MockStorageAdapter` / `InMemoryStorageAdapter` による自律テスト環境を提供し、`npm run check` の全テスト（13ファイル67件以上）がモック環境下で完全オフライン自律動作することを保証。 |

---

## Phase 14: スマートフォン向けレスポンシブUI最適化 & モバイル表示崩れの修正 (Issue #3)

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | モバイル画面（幅320px〜480px）において、ヘッダーの長文テキストタブによる横溢れ、ルート要素の `overflow-hidden` による縦スクロール不能、テーブルのはみ出しが発生するリスク。 | Tailwind のブレークポイント（`md:`, `lg:`）を活用。ヘッダーはモバイルでアイコン表示＋ツールチップ/短縮ラベル化。メインコンテンツは `overflow-y-auto` を適切に配置し、テーブルには `overflow-x-auto` を付与。 |
| **2. UX・エッジケース** | スマートフォンでの片手操作時、タップ領域（44px以上）の確保と、入力フォーム（テキストエリア）と評価結果プレビューの切り替えやすさ。 | タブボタンの最小タップサイズを確保し、入力画面（InputPane）と評価結果（PreviewPane）がモバイルでも縦スクロールで自然に遷移・閲覧できるようにレイアウト調整。 |
| **3. 永続性・互換性** | UI レスポンシブ化に伴い、既存のデータモデル（`JobAnalysisResult`）やローカルストレージ・Markdown 保存の処理に悪影響を与えないこと。 | コアロジック（`src/core/`）およびストレージ層（`src/services/`）は一切変更せず、プレゼンテーション層（`src/components/`, `src/App.tsx`）のみを純粋にレスポンシブ化。 |
| **4. テスト容易性 & 自律性** | レスポンシブ化後も既存の 67 件の単体・UI テストがすべて PASS し、モバイル対応コンポーネントが Vitest / jsdom 環境で安全にレンダリング・動作検証可能か。 | 既存の UI テスト（`Header.test.tsx`, `JobDashboard.test.tsx`, `PreviewPane.test.tsx` 等）を維持し、`npm run check` の全テスト・ビルド検証を 100% 自動パスさせる。 |

---

## Phase 13: 求人元データからの個別/順次バッチAI再評価 & 評価履歴タイムライン保持機能

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | 複数求人の再評価時にLLMを1プロンプトにまとめると詳細分析の精度が低下するリスク。また大量呼び出しによるRPM/TPM制限やタイムアウト。 | 1求人1リクエストの独立した高精度推論を厳格に維持。ダッシュボードでの複数選択に最大5件の上限を設け、クライアント側で1件ずつ順次（シーケンシャルキュー）実行。 |
| **2. UX・エッジケース** | プロファイル更新後に過去求人のスコアがどう変わったか確認したい。途中でエラーが起きた場合でも処理済み求人を保持したい。元テキストが空の場合のケア。 | プレビュー画面・一覧での個別再評価ボタンと、選択バッチ再評価プログレスモーダル（完了件数/中止ボタン）を提供。1件ごとに即時ローカル保存。元テキスト欠落時は詳細情報から再構築。 |
| **3. 永続性・互換性** | 過去のAI評価内容（スコア推移、4軸内訳、ポジティブ、懸念点）の消失防止と既存データ構造（`JobAnalysisResult`）との後方互換性。 | `EvaluationHistoryItem` 型を追加し、再評価前の評価スナップショットを配列で永続化。Markdown にも「`## 📜 適合度評価履歴`」セクションを自動追記。 |
| **4. テスト容易性 & 自律性** | 再評価ロジック、履歴スナップショット蓄積、順次実行キュー、UIプログレス表示が Vitest で 100% 自律自動テスト可能か。 | `MockAiProvider` に再評価スナップショット生成を組み込み、`tests/services/aiService.test.ts`, `tests/features/JobDashboard.test.tsx`, `tests/features/PreviewPane.test.tsx` で全件テストを自動化。 |

---

## Phase 12: ユーザー志向に応じた4軸評価の動的重み付けカスタマイズ＆保存済求人の一括・リアルタイム再計算機能 (Issue #1)

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | スコア再計算のたびに LLM API を呼び出すと通信遅延とトークン費用が発生するリスク。合計100%の整合性維持。 | 保存済みの各軸 0〜100% 生スコア（`scoreBreakdown`）を入力とする純粋関数 `recalculateScoreWithWeights` を実装し、0.01秒のゼロコスト決定論的再計算を実現。合計100%自動調整バランサーを導入。 |
| **2. UX・エッジケース** | 「リスキリング重視」「カルチャー重視」等のプリセットをワンクリックで切り替えたい。求人詳細やダッシュボードでの即時シミュレーション。 | プリセット選択ボタン（🎯標準 / 🚀リスキリング / 🌿カルチャー / 💰待遇 / ⚙️カスタム）を配置。プレビュー画面および一覧で視点切り替えピルを提供し、リアルタイムにスコア・判定ランク・ソート順を連動表示。 |
| **3. 永続性・互換性** | 重み設定（`scoringWeights`）の保存と、過去に作成されたプロファイル・求人データ（`JobAnalysisResult`）との後方互換性。 | `UserProfile.conditions.scoringWeights` にオプショナルとして定義し、未設定時はデフォルト（40/30/20/10%）を自動適用。「全求人に新しい重みを一括適用」ボタンで Markdown Frontmatter も安全に一括更新。 |
| **4. テスト容易性 & 自律性** | プリセットごとのスコア計算、合計100%バランサー、一括再計算、UI視点切り替えが Vitest で 100% 自律自動テスト可能か。 | `tests/core/scoringEngine.test.ts`, `tests/features/PreviewPane.test.tsx`, `tests/features/JobDashboard.test.tsx`, `tests/features/ProfileSettingsView.test.tsx` に単体・UIテストを追加。 |

---

## Phase 11: ポートフォリオ公開用 README.md の刷新 & エンジニアリングスキル証明

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | プロジェクトの技術選定理由（Tauri v2 + React 18 + TS + Gemini API + Vitest）とアーキテクチャ設計（Clean Architecture）が客観的・構造的に伝わること。 | アーキテクチャ図、多軸スコアリング設計、Gemini Structured Outputs、デュアルストレージ設計を分かりやすい Markdown 図解で明記。 |
| **2. UX・エッジケース** | GitHub を訪問した採用担当者・エンジニアが、1分で「プロダクトの価値」「機能の網羅性」「品質水準」を理解できる構成にすること。 | 課題と解決（Problem & Solution）、主要機能一覧、画面構成、開発ハーネス（シークレットスキャン/Doc整合性/Pre-push hook）の防壁設計を魅力的にビジュアル配置。 |
| **3. 永続性・互換性** | リポジトリ内の既存ドキュメント（`docs/`）やテストコードへの相対リンク・参照が壊れていないこと。 | 正確な GitHub Markdown 記法と相対パスでドキュメントへの導線を整備。 |
| **4. テスト容易性 & 自律性** | README 内に記載された起動コマンド、品質ゲートコマンド（`npm run check`）が完全一致し、第三者がクローンしても即座に全テストパスできること。 | `npm install` から `npm run check`, `npm run dev` までの手順を動作確認済みのコマンドで記載。 |

---

## Phase 10: 転職ロードマップのデータ整合性・名寄せ重複排除・出所会社別明記 & 更新機能新設

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | 表記ゆれ（例: `AWS SAA`, `AWS Certified Solutions Architect - Associate` 等）による資格の重複集計、および同一企業による重複カウントの排除。 | 主要資格の正規化マッピング辞書を導入し、同一求人内・同一企業内での重複カウントを Set で厳密排除。 |
| **2. UX・エッジケース** | 「どの会社が必須として求めているのか」「どの会社がアピール推奨と言っているのか」が混同せず一目でわかること。ロードマップ手動更新ボタンの設置。 | 各資格カードに「🏢 企業ごとの指定文脈（必須指定 / 経験補強の推奨）」を企業名バッジとともに個別明記。ヘッダー右上に「🔄 データを再集計・更新」ボタンを新設。 |
| **3. 永続性・互換性** | 保存求人データの構造（`qualificationAdvice`, `careerTrajectory`）に変更を加えることなく、ロードマップ表示ロジック側で柔軟かつ高精度に集計すること。 | 既存の `JobAnalysisResult` 型との完全互換を維持し、集計アルゴリズム（`useMemo`）を安全に強化。 |
| **4. テスト容易性 & 自律性** | 表記ゆれ名寄せ、企業別文脈分類、手動更新ボタンの押下挙動が Vitest で 100% 自動テスト可能か。 | `tests/features/CareerRoadmapView.test.tsx` に名寄せ・企業別明記・更新ボタンの単体UIテストを追加。 |

---

## Phase 9: キャリア展望の常設深掘り・再生成とプロンプト最適化

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | 初回一括生成のレスポンス速度（3秒台）を犠牲にせず、専用プロンプトによる詳細な深掘り分析を提供すること。 | 初回一括解析時はスマートな要約版を高速出力し、プレビュー画面右上に「🔄 AIでキャリア展望を再生成・深掘り」ボタンを常設配置。専用プロンプト（`buildCareerTrajectoryPrompt`）で高密度・具体的な分析結果へ即時アップグレード可能にする。 |
| **2. UX・エッジケース** | 展望がすでに存在する場合でも、再生成ボタンを押した際にローディング状態を表示し、スムーズに最新結果へ差し替わること。 | `isGeneratingTrajectory` ローディング状態とスピナーを表示。再生成完了時は Markdown ドキュメントも自動同期更新しローカルストレージへ即時反映。 |
| **3. 永続性・互換性** | 再生成された `careerTrajectory` がローカル保存データおよびエクスポート用 Markdown に欠落なく永続化されること。 | `generateJobMarkdown` で新展望を反映した Markdown を自動再生成し、`saveJob` で自動保存。 |
| **4. テスト容易性 & 自律性** | 再生成トリガー、UI ローディング挙動、専用プロンプトのテストが Vitest で自律実行可能か。 | `tests/features/PreviewPane.test.tsx` に再生成ボタンの押下・コールバック呼び出しテストを追加。 |

---

## Phase 8: 中長期キャリア展望・獲得スキル・次の転職先 (Career Trajectory) AI & 可視化

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | Gemini API のプロンプトおよび JSON Schema への `career_trajectory` オブジェクト追加と推論遅延の抑制。 | `career_trajectory` (獲得スキル, 次のキャリア選択肢, 将来想定年収, キャリア留意点) を Gemini JSON Schema に定義。既存の高速 Flash モデル（3.6/3.5）で数秒以内に安定生成。 |
| **2. UX・エッジケース** | 「入社後のキャリアの広がり」が視覚的にわかりやすく、単なる転職判断だけでなく 3〜5 年のキャリア戦略ツールとして機能すること。 | プレビュー画面に「🚀 入社後のキャリア展望 & 次の転職先」リッチカードを配置。ロードマップ画面にも「キャリア分岐 & 将来年収シミュレーション」を連動。 |
| **3. 永続性・互換性** | 過去に保存・インポートされた求人データに `careerTrajectory` が存在しない場合のフォールバック。 | `careerTrajectory?: CareerTrajectory`（オプショナル）として定義し、既存データとの完全な後方互換性を保証。Markdown にも専用セクションを自動生成。 |
| **4. テスト容易性 & 自律性** | `MockAiProvider` でのキャリア展望シミュレーション生成、および UI 表示の自動テスト化。 | `MockAiProvider` に業種・職種・年収に応じたキャリアパス生成ロジックを実装し、Vitest で 100% 自律単体テストを担保。 |

---

## Phase 7: 自動ローカル保存・再表示・AI再評価・MDインポート・横並び比較 & 転職ロードマップ画面

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | Markdown (`.md`) インポート時の YAML Frontmatter パースと、AIへのフィードバック付き再評価プロンプトの設計。 | 既存の `parseJobMarkdown` を拡張してドラッグ＆ドロップ/ファイル選択に対応。AI 再評価用プロンプトビルダー（前回評価＋ユーザーフィードバック）を新設。 |
| **2. UX・エッジケース** | 解析完了時に手動保存を押さなくても自動でローカル保存される安心感。保存済み案件のワンクリック再表示。複数案件の横並び比較。選考状況（応募検討中/面接/見送り等）と不足資格を俯瞰するロードマップ画面。 | ① 解析完了時の自動ローカルストレージ保存（ドラフト同期）、② ダッシュボードからの1クリック再プレビュー復元、③ プレビューペイン内の「💡 フィードバックして再評価」フォーム、④ ドキュメントインポートモーダル、⑤ 横並び比較マトリクス、⑥ 新ナビゲーション「ロードマップ」タブの新設。 |
| **3. 永続性・互換性** | ユーザーフィードバック履歴や更新日時が Markdown Frontmatter およびローカルストレージの両方で正しく永続化されること。 | `JobAnalysisResult` に `feedbackHistory?: Array<{ date: string; feedback: string; scoreDelta: number }>` を追加し、既存データとの完全互換を維持。 |
| **4. テスト容易性 & 自律性** | Markdown インポート、AI 再評価ロジック、ロードマップ集計ロジックが、APIキーなしのローカル環境でも 100% 単体テスト可能か。 | `MockAiProvider` に再評価ロジックを組み込み、Vitest でインポートパース・フィードバック再評価・ロードマップ集計を 100% 自動テスト化。 |

---

## Phase 6: スキル・資格ステータス管理 & 資格推薦AI & 状態同期・接続テスト

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | `ProfileSettingsView` でのプロファイル保存が `App.tsx` に即時反映されず `Mock モード` のままになる状態管理の不整合。 | `App.tsx` から `profile` および `saveProfile` を `ProfileSettingsView` に渡し、メモリ上の状態とストレージを単一ソースで同期。 |
| **2. UX・エッジケース** | APIキーが正しいか保存前に確認できない不安、資格・スキルの「取得済」と「学習中・目標」の混同。 | Gemini API 接続テストボタン（疎通確認）を新設。資格・スキルにステータス（取得済/学習中/目標）を追加し、タブ切り替えUIで直感的に登録。 |
| **3. 永続性・互換性** | 資格・スキル構造の拡張に伴う既存プロファイル（LocalStorage/Tauri FS）の後方互換性。 | 既存データに `status` がない場合はデフォルト値（`acquired` / `experienced`）を自動補完しマイグレーション。Markdown Frontmatter にも推奨資格プロパティを互換拡張。 |
| **4. テスト容易性 & 自律性** | API接続テストや資格アドバイス抽出ロジックが、APIキーなしのローカル環境でも 100% 単体テスト可能か。 | `MockAiProvider` にも `qualificationAdvice` モック生成を実装し、Vitest で完全ローカル自律テストを保証。 |

---

## Phase 5: 求人管理ダッシュボード & 複数比較 & 総合テスト

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | 大量案件保存時の検索・ソート処理の軽量化と、比較マトリクスの横スクロール表示。 | React メモ化フィルタリングと、グリッド/テーブルのレスポンシブ切り替え。 |
| **2. UX・エッジケース** | 0件時のガイド表示、ステータス変更時の即時永続化、比較対象（最大3件）の選択上限制御。 | ステータス更新フック (`updateJobStatus`) と選択バッジ・比較モーダル/パネル。 |
| **3. 永続性・互換性** | ステータス変更が Markdown メタデータおよびストレージの両方で整合性を保つこと。 | `storageAdapter.saveJob` でメタデータと Frontmatter を同期更新。 |
| **4. テスト容易性 & 自律性** | 検索・フィルタ・ソート・比較ロジックが自動テスト可能か。 | フィルタ・比較ロジックの単体テストおよびダッシュボード UI テストを Vitest で自動化。 |

---

## Phase 4: Markdown生成 & リッチプレビュー & Vault保存

| 検証軸 | 検出事項・考慮点 | 実装・設計での解決策 |
| :--- | :--- | :--- |
| **1. 技術・環境** | Frontmatter (YAML) を react-markdown で綺麗に表示・分離する必要性。 | `remark-frontmatter` を適用し、YAML と Markdown 本文を視覚的に整理してレンダリング。 |
| **2. UX・エッジケース** | 企業名・職種に OS 禁止文字 (`/`, `\`, `:`, `*`, `?`, `"`, `<`, `>`) が含まれるリスク。 | `getStandardMarkdownFilename` で厳格サニタイズを実施し、不正ファイル名による保存失敗を防止。 |
| **3. 永続性・互換性** | Obsidian, Logseq 等の Vault 直下に直接保存した際、タグやプロパティが即座に認識される互換性。 | 標準 YAML Frontmatter（配列タグ・日付）書式に厳密準拠。 |
| **4. テスト容易性 & 自律性** | エディタ双方向編集やファイル書き出しが自動テスト可能か。 | サニタイズ・パースの単体テストおよびエディタUIテストを Vitest で 100% ローカル完結。 |
