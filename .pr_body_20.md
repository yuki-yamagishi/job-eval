## 概要
地下鉄や機内モードなどの完全オフライン環境であっても 0 秒で即座にアプリを起動し、過去の求人データ閲覧や操作を行えるようにするため、Web App Manifest、Service Worker オフラインキャッシュ、アプリアイコン、PWA ガイド UI を導入しました。

Closes #20

## 主な変更点
1. **Web App Manifest & アプリアイコン (`public/manifest.json`, `public/icons/icon.svg`)**:
   - PWA 標準規格に準拠した Manifest（standalone, theme_color, background_color, maskable icons）を定義。
2. **Service Worker による 0 秒オフライン起動 (`public/sw.js`)**:
   - Cache-First / Stale-While-Revalidate 戦略により、完全圏外でもローカルキャッシュから 0 秒で起動。
   - 動的同期 API（`/api/*`）はバイパス。
3. **PWA メタタグ & Service Worker 自動登録 (`index.html`)**:
   - iOS Safari 用の apple-mobile-web-app-capable, apple-touch-icon を配備。
4. **同期モーダルへの PWA ガイド UI 追加 (`src/components/sync/SyncModal.tsx`)**
5. **ADR-0010 の策定 (`docs/adr/0010-pwa-offline-caching-and-installability.md`)**
6. **単体テストの拡充 (`tests/core/pwaManifest.test.ts`)**

## 品質ゲート検証
- `npm.cmd run check`（シークレットスキャン、ドキュメント整合性、TypeScript型検査、全単体UIテスト 18 ファイル 86 件、Vite本番ビルド）100% PASS 確認済。
