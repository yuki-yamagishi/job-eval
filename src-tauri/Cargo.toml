[package]
name = "job-eval"
version = "0.1.0"
description = "AI Job Evaluation & Markdown Manager Desktop App"
authors = ["JobEval Team"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }

[dependencies]
tauri = { version = "2.0.0", features = [] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
